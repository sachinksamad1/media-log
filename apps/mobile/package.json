{
  "name": "media-log-mobile",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "flutter run",
    "dev:linux": "flutter run -d linux",
    "dev:web": "flutter run -d chrome",
    "build": "flutter build apk",
    "generate": "flutter pub run build_runner build --delete-conflicting-outputs",
    "clean": "flutter clean"
  }
}
