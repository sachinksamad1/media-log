// import { vi } from 'vitest';

// Set test environment variables
process.env.NODE_ENV = 'test';

// Global mocks if needed
// vi.mock('firebase-admin', () => ({ ... }));

// Silence console during tests if desired (optional)
// console.log = vi.fn();
// console.error = vi.fn();
