# Firebase Config (using FB_ prefix to avoid reserved FIREBASE_ prefix)
FB_PROJECT_ID='-----'
FB_CLIENT_EMAIL='-----'
FB_PRIVATE_KEY='-----'
FB_STORAGE_BUCKET='-----'

# Server Config (PORT is reserved by Firebase)
BASE_URL='-----'

# SMTP Configuration
SMTP_HOST='-----'
SMTP_PORT='-----'
SMTP_USER='-----'
SMTP_PASS='-----'
SMTP_SECURE=true
EMAIL_FROM_NAME='-----'
EMAIL_FROM='-----'

# CORS Configuration
# Comma-separated list of allowed origins (no wildcards in production!)
CORS_ALLOWED_ORIGINS='http://localhost:3000,http://localhost:5173'
# Set to 'true' to allow preview/staging URLs matching the patterns below
CORS_ALLOW_PREVIEW_URLS='false'
# Comma-separated regex patterns for preview URLs (without regex delimiters)
# Example: "^https://myproject--.*\\.web\\.app$,^https://.*-myproject\\.vercel\\.app$"
CORS_PREVIEW_PATTERNS=''